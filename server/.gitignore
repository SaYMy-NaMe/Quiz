node_modules/
# single environment file — holds the Atlas credentials, never commit it
.env
uploads/
dist/
coverage/
*.log
.DS_Store
# iCloud Desktop sync conflict copies
* 2*
* 3*
* 4*
# embedded dev database (MONGODB_URI=local)
data/
