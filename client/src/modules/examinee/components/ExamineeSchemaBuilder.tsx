/** Placeholder — the full Google-Forms-style builder lands in the examinee-schema stage. */
export function ExamineeSchemaBuilder(_props: { errors: Record<string, Record<string, string>> }) {
  return (
    <div className="card">
      <p className="muted">Examinee field builder coming in the next stage.</p>
    </div>
  );
}
